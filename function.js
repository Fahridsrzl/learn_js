// funtion itu potongan code yang bisa kita gunakan kembali
function lembarDadu(){
    console.log(Math.floor(Math.random() * 6 ) + 1);
}

// cara bikinnya mudah
function namaFunction() {
    // lakukan proses
};

// buat manggil
namaFunction();

// parameter dan argument
// parameter sama argument ini adalah input
function greet (name)//ini parameter)
{
    console.log(`hai, ${nama}`)
};

greet ('jason'); // ini argument

//return
//mengakhiri proses di function

//scope
//variabel ny cuma di didalam function kalo yang diluar ya beda

